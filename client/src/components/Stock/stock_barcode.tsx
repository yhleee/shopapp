import * as React from 'react'
import { styling } from 'common/utils'
import * as s from './stock.scss'

interface OwnProps {}

interface OwnState {}

class StockBarcode extends React.Component<OwnProps, OwnState> {
  constructor(props) {
    super(props)
  }

  render() {
    return <></>
  }
}
export default styling(s)(StockBarcode)
